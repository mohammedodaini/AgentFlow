"""Metrics: request counts/latency histograms, agent tokens & cost counters.

Prometheus-style /metrics at M16; counters get wired earlier as features land.
"""

from __future__ import annotations

# TODO(M16): metric registry + FastAPI instrumentation + agent cost counters
