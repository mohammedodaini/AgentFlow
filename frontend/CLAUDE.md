@AGENTS.md

<!--
The block above is Next's own, regenerated by `next dev`, leave it.

Project memory lives in the repository root: ../CLAUDE.md. Read that first; it
carries the working agreement, the milestone history, and the decisions this
frontend is built on top of.

The one thing to know before changing anything here: the browser never holds a
JWT. See ./README.md and ../docs/adr/0016-the-browser-never-holds-a-jwt.md.
-->
