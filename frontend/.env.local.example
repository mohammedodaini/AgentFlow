# The backend this frontend proxies to. Server-only on purpose — no
# NEXT_PUBLIC_ prefix, because Next inlines those into the browser bundle and
# the browser is never meant to call the API directly (see src/lib/session.ts).
BACKEND_URL=http://localhost:8000
